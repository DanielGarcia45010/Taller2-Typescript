export class Student {
    code: string;
    id: string;
    age: number;
    adress: string;
    number: string;
  
    constructor(code: string, id: string, age: number, adress: string, number: string) {
      this.code = code;
      this.id = id;
      this.age = age;
      this.adress = adress;
      this.number = number;

    }
  }